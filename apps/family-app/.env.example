# Leave empty for local dev (auto-resolves from Expo dev server host)
# Set these for deployed backends (include https:// prefix):
EXPO_PUBLIC_USERDATA_API_URL=
EXPO_PUBLIC_MEMORY_API_URL=
EXPO_PUBLIC_REMINDERS_API_URL=
